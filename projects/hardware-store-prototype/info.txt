# Setup - Development

Set up docker network and volume
CMD "docker compose up --build setup"
CMD "docker compose up --build back"
CMD "docker compose up --build front"

# Setup - Production

???

# Data structures

Product:
    name: string
    price: int
    stock: int
    description: int
    image: Img

# Preparations for deployment

* Revise the docker-compose.yaml and Dockerfile files
* Revise all comments with the term "DEVELOPMENT"
* Revise all comments with the term "TODO"
